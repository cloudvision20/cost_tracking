{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run Client: start",
            "request": "launch",
            "command": "npm run start",
            "cwd": "${workspaceFolder}\\client"
        },
        {
            "type": "node-terminal",
            "name": "Run Server: dev",
            "request": "launch",
            "command": "npm run dev",
            "cwd": "${workspaceFolder}\\server"
        }
    ]
}